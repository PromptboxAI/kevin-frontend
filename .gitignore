# Repo snapshot archives — never commit (GitHub hard-limits any file at 100 MB)
*.zip

# Loose camera dumps at the design/ root — unreferenced duplicates of assets/claim/
design/20*_*.jpg

# OS cruft
.DS_Store
Thumbs.db

# --- Vite / Node app (scaffolded at repo root) ---
node_modules
dist
dist-ssr
*.local
logs
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea
*.sw?

# Local dev tooling
.claude/

# Windows shortcuts
*.lnk
